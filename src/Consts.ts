export const REQUIRED = (item: string) => (`${item}は必須です.`);
export const USER_CREATED = (username: string) => (`${username} は作成しました`);
export const USER_DELETED = (username: string) => (`${username} は削除しました`);
